name: Deploy GAS

on:
  push:
    branches: [main]
    paths:
      - 'workspace/gas/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install clasp
        run: npm install -g @google/clasp

      - name: Set up clasp credentials
        run: echo '${{ secrets.CLASPRC_JSON }}' > ~/.clasprc.json

      - name: Push to GAS
        working-directory: workspace/gas
        run: clasp push --force
